// 1. Подключение заголовочного файла
#include "../common/s21_utils.h" // Директива препроцессора

/* Что это: Директива препроцессора. Она говорит компилятору: «Возьми всё содержимое файла s21_utils.h, который лежит на уровень выше (..), в папке common, и вставь его сюда перед началом компиляции».
Зачем нам это (из ТЗ):
В задании сказано: «Необходимо исключить дублирование кода, переиспользовать общие модули между утилитами. Общие модули можно вынести в отдельную папку src/common».
В этом заголовочном файле описаны:
Структура CatFlags (для хранения состояний флагов -n, -e, -s и т.д.).
Прототипы функций parse_cat_flags (парсинг аргументов) и execute_cat (основная логика работы).
Подключения стандартных библиотек (stdio.h, stdlib.h, stdbool.h и др.), чтобы не писать их в каждом .c файле заново.
Глубокий смысл: Мы разделяем интерфейс (.h) и реализацию (.c). Главный файл s21_cat.c становится очень легким и читается как оглавление программы: «Инициализировать -> Разобрать флаги -> Найти файлы -> Выполнить». Вся «грязная» работа спрятана в common. */

// 2. Точка входа в программу
int main(int argc, char *argv[]) {
  // Что это: Функция main — единственная точка входа для любой C-программы. Операционная система вызывает именно её.
  // Параметры:
  // int argc (Argument Count): Целое число, показывающее, сколько всего аргументов было передано программе при запуске (включая имя самой программы).
  // char *argv[] (Argument Vector): Массив строк (массив указателей на символы), содержащий сами аргументы.
  // argv[0] — всегда имя программы (например, ./src/cat/s21_cat).
  // argv[1] ... argv[n] — флаги и имена файлов, которые ввел пользователь.
  // Возвращаемый тип int: Программа должна вернуть код завершения операционной системе. 0 — успех, любое другое число (обычно 1) — ошибка. Это требование POSIX и часть профессиональной разработки утилит.

  if (argc < 2) {
    return 0;
  }
  // Логика: Если аргументов меньше 2, значит, пользователь запустил программу без параметров (только argv[0] — имя программы).
  // Пример: пользователь ввел просто ./s21_cat и нажал Enter.
  // Зачем: 
  // Утилита cat предназначена для вывода файлов. Если файлов нет, ей нечего делать.
  // В ТЗ сказано: «Ввод через stdin обрабатывать не обязательно». Поэтому мы не запускаем бесконечный цикл чтения из консоли, а просто корректно завершаем работу.
  // Почему return 0, а не ошибка? Стандартное поведение многих Unix-утилит при отсутствии аргументов — просто ничего не сделать и выйти успешно (код 0), чтобы не ломать скрипты, которые могут вызывать их условно. Хотя некоторые версии cat могут ждать stdin, наше ТЗ позволяет нам упростить это до тихого выхода.
  
  // 4. Создание структуры флагов
  CatFlags flags;
    // Что это: Объявление переменной flags типа CatFlags.
    // Что внутри: Эта структура (описанная в s21_utils.h) выглядит примерно так:

/*   typedef struct {
    bool b; // флаг -b
    bool e; // флаг -e
    bool n; // флаг -n
    bool s; // флаг -s
    bool t; // флаг -t
    bool v; // флаг -v
    // ... и другие
  } CatFlags; */

  // Зачем: Нам нужно где-то хранить состояние программы. Пользователь мог ввести cat -n -e -s file.txt. Нам нужно запомнить, что -n включен, -e включен и т.д., чтобы передать эту информацию в функцию обработки. Использование структуры — это принцип структурного программирования: группировка связанных данных.
  // Важный нюанс: На этом этапе переменная flags содержит «мусор» (случайные значения из памяти), так как мы её еще не инициализировали. Инициализация произойдет в следующей строке.

  // 5. Парсинг (разбор) аргументов
  parse_cat_flags(argc, argv, &flags);

  // Что делает: Вызывает функцию, которая пробегает по всем аргументам командной строки (argv), ищет те, что начинаются с минуса (-), и устанавливает соответствующие поля в структуре flags в true.
  // Почему &flags: Мы передаем адрес переменной flags (оператор & — взятие адреса).
  // В C аргументы передаются по значению (копируются). Если бы мы передали просто flags, функция изменила бы копию, и оригинал в main остался бы с мусором.
  // Передавая указатель (CatFlags *), мы даем функции доступ к оригинальной памяти. После возврата из функции переменная flags в main будет заполнена правильными значениями (например, flags.n = true).
  // Связь с ТЗ: Реализует поддержку всех флагов (-b, -e, -E, -n, -s, -t, -T, -v), требуемых в Part 1.

  // 6. Поиск первого файла
  int file_start_idx = 1;
  while (file_start_idx < argc && argv[file_start_idx][0] == '-') {
    file_start_idx++;
  }

/*Цель: Определить, где в списке аргументов заканчиваются флаги и начинается первый файл.
  Логика по шагам:
  int file_start_idx = 1; — Начинаем проверку со второго аргумента (индекс 0 — это имя программы).
  while (...) — Цикл работает, пока выполняются два условия:
  file_start_idx < argc: Мы не вышли за пределы массива аргументов.
  argv[file_start_idx][0] == '-': Первый символ текущего аргумента — это минус. Значит, это флаг.
  file_start_idx++; — Если это флаг, увеличиваем индекс и проверяем следующий.
  Пример работы:
  Команда: s21_cat -n -e -s test.txt log.txt
  i=1: -n (начинается с -) -> i становится 2.
  i=2: -e (начинается с -) -> i становится 3.
  i=3: -s (начинается с -) -> i становится 4.
  i=4: test.txt (начинается с t, а не -) -> цикл останавливается.
  Результат: file_start_idx = 4. Теперь мы знаем, что файлы начинаются с 4-го элемента.
  Зачем это нужно: Функция execute_cat должна работать только с файлами. Ей не нужно знать про флаги (они уже записаны в структуру flags). Мы должны отделить «зерна от плевел». */

  // 7. Проверка наличия файлов
  if (file_start_idx >= argc) {
    return 0;
  }
  
/*Ситуация: Представьте, что пользователь ввел: s21_cat -n -e -s.
  Цикл while прошел все аргументы, так как все они были флагами.
  file_start_idx стал равен argc (количеству аргументов).
  Смысл: Если индекс первого файла больше или равен общему количеству аргументов, значит, файлов не было указано вообще.
  Действие: Как и в начале функции, мы просто выходим с кодом 0. Нечего обрабатывать — программа завершается. Это защищает нас от попытки открыть несуществующий файл с индексом argc (что привело бы к падению программы).
  // Вызываем функцию и сразу возвращаем её результат (0 или 1) */

  // 8. Запуск основной логики и возврат результата
  return execute_cat(&flags, file_start_idx, argc, argv);

/* Вызов функции: Мы передаем управление функции execute_cat, которая находится в src/common/s21_utils.c.
  Аргументы:
  &flags: Настройки (какие флаги включены).
  file_start_idx: Индекс, с которого начинается список файлов.
  argc, argv: Полные данные об аргументах (нужны, чтобы функция могла пробежаться по списку файлов до конца).
  Ключевое слово return:
  Функция execute_cat возвращает int (код ошибки).
  Внутри execute_cat есть логика: если хотя бы один файл не удалось открыть (fopen вернул NULL), она меняет локальную переменную error_status на 1.
  Возвращая это значение из main, мы сообщаем операционной системе (или скрипту, который запустил нашу программу), что произошла ошибка.
  Связь с ТЗ:
  Требование: «Текст сообщения в случае ошибки не имеет значения» (мы выводим его в execute_cat через fprintf(stderr, ...)).
  Но код возврата важен для автоматических тестов (как наш run_tests.sh в тесте №13). Если программа вернет 0 при ошибке, тест упадет. Поэтому мы пробрасываем статус ошибки наружу.
} */

/* Резюме для s21_cat.c
Этот файл — идеальный пример структурного программирования и декомпозиции:
Он не занимается «грязной работой» (чтением файлов, проверкой символов, нумерацией строк).
Его задача — быть диспетчером:
Принять аргументы.
Проверить их валидность (есть ли что обрабатывать).
Разобрать настройки (parse_cat_flags).
Найти данные (while цикл для поиска файлов).
Передать управление исполнителю (execute_cat).
Вернуть результат работы системе.
Такой подход делает код читаемым, поддерживаемым и легко тестируемым. Если завтра нужно будет изменить логику парсинга, мы полезем только в s21_utils.c, не трогая main. */