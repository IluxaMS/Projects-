// Файл s21_utils.h — это фундамент и интерфейс всего вашего проекта. Если .c файлы — это «мышцы» (логика), то этот заголовочный файл — «скелет» и «нервная система», 
// которые связывают всё воедино. Без него компилятор не знал бы, какие структуры данных использовать и какие функции вызывать.

// 1. Защита от повторного включения (Include Guard)
#ifndef S21_UTILS_H
#define S21_UTILS_H

// Проблема: В C, если вы подключите один и тот же заголовочный файл дважды (например, s21_cat.c включает его напрямую, а s21_utils.c тоже), компилятор попытается объявить структуры и функции дважды. Это вызовет ошибку компиляции (redefinition of ...).
// Решение:
// #ifndef S21_UTILS_H: «Если макрос S21_UTILS_H не определен...»
// #define S21_UTILS_H: «...то определи его прямо сейчас и пропусти код ниже».
// При втором включении макрос уже будет определен, и компилятор пропустит всё содержимое до #endif.
// Итог: Гарантирует, что код внутри файла будет обработан компилятором ровно один раз за сборку.

// 2. Макрос POSIX
#define _POSIX_C_SOURCE 200809L

// Зачем здесь? Этот макрос должен быть определен до подключения любых системных заголовков (stdio.h, stdlib.h и т.д.).
// Суть: Он говорит компилятору: «Включи в стандартные библиотеки функции стандарта POSIX.1-2008».
// Что это дает: Без этой строки функции getline (чтение строки произвольной длины) и strdup (дублирование строки) будут считаться неизвестными при строгом стандарте C11 (-std=c11 -pedantic).
// Размещение их здесь гарантирует, что все файлы, включающие этот заголовок, смогут использовать эти функции.

// 3. Подключение стандартных библиотек
#include <ctype.h> // Функции работы с символами (хотя в коде мы часто делаем это вручную, библиотека может пригодиться).
#include <errno.h> // Глобальная переменная errno и функция strerror для обработки ошибок (например, «файл не найден»).
#include <regex.h> // Критически важно для grep. Библиотека регулярных выражений (regcomp, regexec, regfree).
#include <stdbool.h> // Типы bool, true, false. Делает код читаемым.
#include <stdio.h> // Ввод-вывод (printf, fprintf, fopen, getline, FILE).
#include <stdlib.h> // <stdlib.h>: Память (malloc, free, strdup, memset) и общие утилиты.
#include <string.h> // <string.h>: Работа со строками (strlen, strcmp, strcspn, memset).

// 4. Константа ограничения
#define MAX_PATTERNS 100  // Максимальное количество паттернов -e

// Смысл: Определяет максимальное количество паттернов, которые может обработать grep за один запуск (при использовании множественных -e или чтении из файла -f).
// Почему макрос? Чтобы легко изменить лимит в одном месте, если потребуется. Используется для объявления размеров массивов в структуре GrepFlags.

// 5. Структура CatFlags
typedef struct {
  bool b;
  bool e;
  bool n;
  bool s;
  bool t;
  bool v;
  bool E_flag;
  bool T_flag;
} CatFlags;

// Назначение: Контейнер для хранения состояния утилиты cat.
// Поля: Каждое поле соответствует флагу командной строки.
// bool b: Флаг -b (нумерация непустых).
// bool e: Флаг -e (показывать $).
// bool n: Флаг -n (нумерация всех).
// bool s: Флаг -s (сжатие пустых).
// bool t: Флаг -t (показывать табы).
// bool v: Флаг -v (визуализация непечатных).
// bool E_flag: Специальный флаг для -E (GNU версия -e без -v). Нужен, чтобы отличать поведение от обычного -e.
// bool T_flag: Специальный флаг для -T (GNU версия -t без -v).
// typedef struct { ... } CatFlags;: Позволяет обращаться к типу просто как CatFlags, а не struct CatFlags.

// 6. Структура GrepFlags
typedef struct {
  bool e;
  bool i;
  bool v;
  bool c;
  bool l;
  bool n;
  bool h;
  bool s;
  bool f;
  bool o;

  char *patterns[MAX_PATTERNS];  // Массив паттернов
  int pattern_count;             // Количество паттернов
  regex_t regexes[MAX_PATTERNS];  // Скомпилированные регексы
} GrepFlags;

// значение: Контейнер для состояния утилиты grep. Здесь сложнее, так как нужны не только флаги, но и данные для поиска.
// Булевы флаги:
// e: Паттерн задан явно (-e).
// i: Игнор регистра (-i).
// v: Инверсия (-v).
// c: Только счет (-c).
// l: Только имена файлов (-l).
// n: Номера строк (-n).
// h: Скрыть имена файлов (-h).
// s: Тишина при ошибках (-s).
// f: Паттерны из файла (-f).
// o: Только совпадения (-o).
// Массивы и данные:
// char *patterns[MAX_PATTERNS]: Массив указателей на строки. Здесь хранятся тексты шаблонов поиска. Например, если ввели -e "foo" -e "bar", тут будут лежать указатели на "foo" и "bar".
// int pattern_count: Счетчик, сколько ячеек массива patterns занято.
// regex_t regexes[MAX_PATTERNS]: Массив структур типа regex_t. Сюда библиотека <regex.h> запишет скомпилированные внутренние представления паттернов после вызова regcomp. Мы храним их заранее, 
// чтобы быстро проверять каждую строку файла против всех паттернов.

// 7. Прототипы функций (Интерфейс)
void parse_cat_flags(int argc, char *argv[], CatFlags *flags);
void parse_grep_flags(int argc, char *argv[], GrepFlags *flags);

// Задача: Разобрать аргументы командной строки (argv) и заполнить структуру flags.
// Аргументы:
// argc, argv: Данные от main.
// flags: Указатель на структуру, которую нужно заполнить.

int execute_cat(CatFlags *flags, int file_start_idx, int argc, char *argv[]);
void execute_grep(GrepFlags *flags, int file_start_idx, int argc, char *argv[]);
// Задача: Основная работа утилит (чтение файлов, поиск, вывод).
// Аргументы:
// flags: Настройки (как работать).
// file_start_idx: Индекс в массиве argv, с которого начинаются имена файлов (после всех флагов).
// argc, argv: Полный список аргументов для доступа к именам файлов.
// Возврат:
// execute_cat возвращает int (код ошибки), чтобы main мог вернуть его ОС.
// execute_grep возвращает void (в нашей реализации статус ошибки обрабатывается внутри или не требуется как код возврата для main в текущей версии, хотя обычно тоже полезно возвращать int).

// Утилита вывода
void print_visual_char(unsigned char c, FILE *out);

// Задача: Вывести один символ в визуализированном виде (например, превратить код 9 в строку ^I).
// Аргументы:
// c: Символ (беззнаковый, чтобы охватить весь диапазон 0-255).
// out: Поток вывода (FILE *), куда писать (обычно stdout).

// 8. Завершение защиты
#endif // Закрывает блок #ifndef. Конец файла.

/* Резюме роли s21_utils.h
Этот файл реализует принцип инкапсуляции и разделения интерфейса и реализации:
Централизация данных: Все структуры определены в одном месте. Если нужно добавить новый флаг, вы меняете только этот файл (добавляете поле в struct), и оно становится доступно везде.
Стандартизация: Подключение POSIX и всех библиотек гарантировано для всех модулей проекта.
Связность: Прототипы функций позволяют модулям cat и grep использовать общую логику из utils, не зная деталей её реализации.
Без этого файла вам пришлось бы дублировать определения структур и прототипы в каждом .c файле, что нарушило бы принцип DRY (Don't Repeat Yourself) и сделало бы код хрупким.
Теперь вы видите, что этот небольшой файл — это архитектурный центр вашего проекта, Мой Господин! */