Вот теория которую я изучил по HTML на данный момент:

Старая теория:

1) Тэги div и span
2) Семантические тэги глобальной структуры сайта: header, nav, main, section, article, aside, footer
3) Тэги h1(2, 3, 4, 5, 6), p, q, blockquote, address, mark
4) Тэги ul, ol(и его атрибут reversed), li, dl, dt, dd
5) Тэги img(и его атрибуты - src и alt), picture, source(и его атрибуты media, srcset), figure, figcaption
6) Тэги audio(и его атрибуты src, controls, loop), video(и его атрибуты src, controls, loop, autoplay muted(в связке), poster, preload), их внутренний тэг source(и его атрибуты src и type)
7) Тэги a(и его атрибуты href с просто ссылкой, href как якорная ссылка с передачей id того элемента к которому нужно переместиться(я знаю, что нужно перед id того элемента к которому нужно переместиться, у href якорной ссылки, нужно поставить - #), href как номер телефона, href как email и href как skype) и target со значением _blank(остальные значения я не разбирал) и button(с его атрибутами type(разобрал все три значения(button, reset и submit)), autofocus и disabled(который, как мне кажется не нужно давать в задание, так как он просто выключает кнопку))
8) Тэги table, caption, thead, tbody, tfoot, tr, th, td(и его атрибуты rowspan и colspan)
9) Тэги form(с его атрибутами autocomplete, method(но в нём я не разбирал ни одного значения, поэтому нет смысла давать этот атрибут в задании), name), label(с его атрибутом for для связки с input) + input(с его атрибутами id и type(я пока что разобрал только значение text)) для семантики, а так же второй семантический вид label + input без id и for - это просто input внутри label, select(с его атрибутом name), option(с его атрибутом value), textarea, fieldset и legend

Новая теория:

HTML:

1) Значение атрибута type у тэга input: text, number, password, tel, email, search, file, url, date, datetime-local, month, week, time, radio(+ я знаю, для того, чтобы можно было выбирать только 1 из radio, нужно всем задать одинаковый name). Тэги br, hr и datalist + атрибут list у тэга input. А также значения reset и submit у атрибута type(не надо его мне давать в задании, так как это просто замена кнопкам с тем же значениями)

2) Общие атрибуты: id, class, style(не надо его мне давать в задании, так как я уже начал изучать css), title, tabindex

CSS:

1) Как обращаться к тэгу(просто прописать тэг), к классу(через точку), к id(через решётку), то что в одном атрибуте class может быть несколько значение через пробел, к нескольким элементам стразу(просто через запятую перечислить), к классу у конкретного тэга(написать тэг, после чего обратится через точку или решётку, в зависимости от того, к чему мы обращаемся(к классу или id), к классу или id, главное без пробелов, например "div.block"), к дочерним элементам по классу у конкретного тэга(синтаксис такой же как и у обращения к классу у конкретного тэга, только нужно поставить пробел после названия тэга), к дочерним элементам первого уровня(.класс(или id)>.класс(или id))

2) Специфичность: самый нижний вариант когда обращаемся к одному и тому же тэгу(классу, id и так далее) несколько раз подряд, самый маленький вес, когда мы обращаемся по тэгу(элементу) или псевдоэлементу(я его ещё не проходил) - 1, когда мы обращаемся по классу, атрибуту(я ещё не изучал такое обращение) или псевдоклассу, то вес уже - 10, поэтому они приоритетнее в выборе браузера, чем элементы или псевдоэлементы, когда мы обращаемся по id, то вес уже - 100, когда мы прописываем стиль тэгу через артибут style, то бишь в html, то вес уже - 1000, ещё есть "!important", который мы прописываем тому или иному параметру, чтобы сказать, что тот или иной стиль самый важный, он весит больше > 1000, но его лучше никогда не использовать. А также то, что веса можно комбинировать, например, обращение ".block" весит 10, а обращение "h1.block" уже весит 11, следовательно для браузера "h1.block" приоритетнее, чем ".block"

3) Методолгия БЭМ: блоки - через тире(если два и более слов), элементы(то что внутри блоков) - название болка(то бишь родителя), затем два нижних подчёркивания и название самого элемента, модификатор - название болка(то бишь родителя), затем два нижних подчёркивания, название самого элемента, потом два тире и пишем название модификатора(+ через тире если два и более слов), который характеризует внешний вид, то бишь отвечает на вопросы по типу "какой размер?", "какая тема ?", например "user-card__desc--color-green", и кстати модификатор можнно писать не через два тире, а просто через нижнее подчёркивание

4) Обнуление стилей

5) Свойства редактирования текста: color, font-family(его значение дефолтные шрифты + sans-serif, но я с добавлением шрифтов, из глобальных источников, ещё не работал), font-size(пока что только px знаю), line-height, font-style, font-weight(и цифры и просто слова разобрал), text-align, text-decoration(связки по типу underline + dotted тоже разобрал), text-shadow, text-transform, text-indent, letter-spacing, word-spacing. И вообще я ещё изучил white-space, но почему-то мне доступны только значения "inherit", "initial" и "unset" - скажи почему так

6) Два вида единиц измерения: aбсолютные: px и относительные(em и rem, %, vh и vw, vmin и vmax)

Твоя задача дать мне 1 практическое задание, где будет сочетаться прям вся вышеописанная теория(и старая и новая), без исключения(особенно Тэги dl, dt, dd, так как я ошибся в них в прошлом ТЗ про "Страница музыкального альбома и клипа"). Так же, категорически нельзя выходить за пределы той теории, которую я изучил. В ТЗ не говори, где и какой конкретно тэг мне нужно использовать, то есть просто напиши, что должно быть на сайте, а я уже сам должен додуматься какой тэг лучше всего стоит использовать
